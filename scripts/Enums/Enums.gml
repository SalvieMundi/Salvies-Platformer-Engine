enum gravType {
	directional,
	object
}

enum cameraType {
	smooth,
	snap
}

enum langType {
	english,
	french,
	spanish
}

enum lightingType {
	gaussian,
	retro
}

enum transitionType {
	none,
	fade,
	swipe
}

enum playerState {
	free
}

enum objectState {
	free,
	switchingGravity
}

enum windowMode {
	fullscreen,
	window1x,
	window2x,
	window3x
}